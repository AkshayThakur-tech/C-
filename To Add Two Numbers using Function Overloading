#include <iostream>
using namespace std;

int Sum(int x)
{
    return x;
}

double Sum(double x)
{
   return x; 
}

int main() {
   
   
    cout<<"Sum of 2+3= " << Sum(5);
    
    cout<< "   Sum of 2.3+4.3 = " << Sum(5.5);
    return 0;
}
